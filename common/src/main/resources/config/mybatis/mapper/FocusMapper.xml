<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.decade.gas.mapper.index.FocusMapper">

    <resultMap id="resultFocus" type="org.decade.gas.entity.index.FocusEntity">
        <id property="focusId" column="focus_id" javaType="java.lang.String"/>
        <result property="publishTime" column="publish_time" javaType="java.lang.String"/>
        <result property="updateTime" column="update_time" javaType="java.lang.String"/>
        <result property="createTime" column="create_time" javaType="java.lang.String"/>
    </resultMap>
    <select id="listAll" resultMap="resultFocus">
        SELECT
            id,
            focus_id,
            title,
            content,
            picture,
            info,
            state,
            publish_time,
            update_time,
            create_time
        FROM f_t_focus
        WHERE state = 1
        ORDER BY publish_time DESC
    </select>
    <select id="search" resultMap="resultFocus">
        SELECT id,focus_id,title,content,picture,info,state,publish_time,update_time,create_time
        FROM f_t_focus
        WHERE state=1
        <if test="focusId != null">AND focus_id=#{focusId}</if>
        ORDER BY publish_time DESC
    </select>
    <select id="searchList" resultMap="resultFocus">
        SELECT
            id,
            focus_id,
            title,
            content,
            picture,
            info,
            state,
            publish_time,
            update_time,
            create_time
        FROM f_t_focus
        WHERE state = 1
        ORDER BY publish_time DESC
        LIMIT #{from}, #{to}
    </select>

    <select id="totalCount" resultType="java.lang.Integer">
        SELECT count(id)
        FROM f_t_focus
    </select>


</mapper>